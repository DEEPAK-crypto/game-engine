apiVersion: v1
kind: Secret
metadata:
  name: redis-secrets
  namespace: game-platform
  labels:
    app.kubernetes.io/name: redis
type: Opaque
stringData:
  # Optional: Set password for Redis authentication
  # Uncomment and set a secure password for production
  # password: "REPLACE_WITH_SECURE_PASSWORD"
  password: ""